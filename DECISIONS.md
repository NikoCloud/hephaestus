# DECISIONS.md --- Decision Log (append-only)

| date | decision | why | reversible? |
|---|---|---|---|
| 2026-07-11 | Mojo native, no Vulkan for v1 | No Mojo-SPIR-V backend yet | Yes |
| 2026-07-11 | BF16 before FP8 sequencing | Prove correctness first | Yes |
| 2026-07-11 | Qwen3-4B dense as first arch | Small, known, single-GPU fits | Yes |
| 2026-07-11 | Vendor MAX kernels, do not write | Do not reinvent what exists | Yes |
| 2026-07-11 | 90 percent single-stream bar | Bandwidth bound, batching wins | No |
| 2026-07-11 | GGUF permanently out | Thesis is escaping that gravity well | No |
| 2026-07-11 | torch installed system-wide, not in pixi | pixi resolver cannot solve pytorch-triton-rocm dependency; torch only needed for GGUF converter, not Mojo work | Yes |
| 2026-07-11 | Both cards confirmed gfx1201 via rocminfo | R9700 and 9070 XT are both RDNA4 gfx1201; earlier gfx1150 claim was wrong | No |
| 2026-07-11 | Baseline 55.14 tok/s tg128 = ~69% bandwidth utilization | R9700 has ~512 GB/s peak; 4B model at F16 = ~7.5GB per forward pass; 55 t/s x 7.5GB = ~413 GB/s effective | No |
| 2026-07-11 | Zero-copy weight loading: transpose_b=True + safetensors [out,in] = kernel-ready | safetensors stores weights in PyTorch [out_features, in_features] order; MAX matmul kernel transpose_b=True expects exactly this; no transposition at load time | No |
| 2026-07-11 | Dossier section 6 shapes corrected from GGUF-order to safetensors-order | Original table pulled GGUF shapes (reversed dims); verified against actual safetensors header | No |
| 2026-07-11 | Dossier tensor count corrected: 398 not 434 | 1 + 36x11 + 1 = 398 (11 per layer, not 12); verified from safetensors header | No |
| 2026-07-11 | Scripts executed as files, never piped through interactive shell | cachyos runs fish; base64-smuggling past fish expansion is fragile; committed scripts = debuggable archaeology | Yes |
| 2026-07-11 | Single DeviceBuffer arena for all weights | 398 tensors; avoids allocator fragmentation; one allocation, one lifetime; trivial memory budget calc for Phase 2 | Yes |
| 2026-07-11 | Python pre-pass stages safetensors to flat binary + text offsets | Mojo has no JSON parser; vendoring safetensors reader as Python (scripts/stage_weights.py) rather than writing one | Yes |
| 2026-07-12 | Exp 1 resolved: TileTensor type erasure via Origin-parameterized structs | Struct with [origin: Origin[mut=True]] holds TileTensor fields of differing compile-time layouts using origin=Self.origin; evidence: src/hephaestus/model.mojo compiles with all 11 layouts | Yes |
| 2026-07-12 | Exp 2 resolved: HostBuffer pipeline over mmap for host-to-device copy | enqueue_create_buffer -> enqueue_create_host_buffer -> fill -> enqueue_copy -> synchronize verified working; callers of DeviceContext must be raises; mmap-direct path untested, revisit only if load exceeds 30s gate | Yes |
| 2026-07-12 | Stager drops tied lm_head.weight after byte-identity check vs embed_tokens | tiny_random saves lm_head despite tie_word_embeddings; 4B omits it; dropping at staging gives the Mojo loader one uniform tensor set (2 + 11L) | Yes |
| 2026-07-12 | Model dims are comptime struct params, not hard-coded 4B constants | tiny_random debug loop (mandated) needs different dims; compile-time parameterization, zero runtime config-driven generality; 4B stays the only shipped instantiation | Yes |
| 2026-07-12 | Loader verifies device copy by round-trip compare (full <=16MB, sampled above) | GPU copies fail silently (project has already seen an all-zeros kernel); ~1s overhead on 8GB load, still 6.3s total | Yes |
| 2026-07-12 | G1a-3 PASS: 4B loads in ~6.3s warm, <=8.5s cold bound | 3 reps in bench/1a-load.md; NVMe direct read 4.3 GB/s bounds the cold case | No |
| 2026-07-12 | A12 (metadata format == "pt") enforced in stager, not Mojo loader | staging strips safetensors headers; the stager is the only place the header is visible; verified passing on tiny + 4B | Yes |
| 2026-07-12 | Tiny oracle extended with step logits from EXISTING weights (scripts/build_tiny_logits.py) | tiny had token IDs only; logit-level diffing needs logits; every step token-verified vs reference_outputs.json so transformers drift fails loudly instead of poisoning fixtures | Yes |
| 2026-07-12 | **[SUPERSEDED 2026-07-13 -- resolved via direct llvm_intrinsic; see rows below]** ~~BLOCKER: no WMMA of any dtype compiles on gfx1201 in Mojo 1.0.0b3.dev2026071006~~ | BF16 16-elem frags (RDNA3 encoding, what stdlib emits) -> `LLVM ERROR: Cannot select: intrinsic %llvm.amdgcn.wmma.f32.16x16x16.bf16`; BF16 8-elem frags (RDNA4 encoding) -> stdlib comptime assert "no valid implementation"; FP8 E4M3 8- and 16-elem -> `LLVM ERROR: Do not know how to split this operator's operand!`. Evidence: experiments/exp3c,exp3d,exp3f. stdlib RDNA WMMA is gfx11-only (mma_amd_rdna.mojo:370 hard-asserts `_has_shape[(16,16,8,8)]`; buffers.mojo:39 `RDNA_AB_FRAG_SIZE = 16`) | No (upstream toolchain) |
| 2026-07-12 | Consequence: high-level `matmul()` dispatcher unusable for BF16 on gfx1201 | it instantiates `gemm_kernel_rdna` at comptime regardless of the runtime m/n branch, so even an M=1 call that would route to gemv fails to compile (exp3b) | No |
| 2026-07-12 | Consequence: vendored RDNA attention (AttentionRDNA / mha_prefill / mha_decode) also blocked | mha.mojo:2215 `comptime if _is_amd_rdna()` -> AttentionRDNA -> rdna_mma -> the same 16-elem BF16 mma() call proven failing in exp3c. Chain verified in source; the failing unit is verified empirically | No |
| 2026-07-12 | Exp 1 resolved: call WMMA-free kernels directly, not the matmul() dispatcher | `gemv_gpu` (decode M=1) and `matmul_kernel_naive` (prefill M>1) both compile and are correct vs CPU fp32 ref on gfx1201 (exp3e: 0 errors, max_diff 0.031 at bf16 tol). Decode is bandwidth-bound so gemv is the G1a-2 path anyway; prefill perf is not gated in 1a | Yes |
| 2026-07-12 | Exp 2 resolved: vendor `LayoutTensorMHAOperand`, do not write a KV wrapper | mha_operand.mojo:873 is "an implementation for contiguous tensor arguments to MHA kernels", rank-4 [batch, tok, head, head_dim] — exactly our single-stream contiguous cache. NOTE: moot until attention is unblocked; the RDNA attention kernels it feeds do not compile | Yes |
| 2026-07-12 | Exp 3 resolved: rms_norm_gpu with multiply_before_cast=False, weight_offset=0 | reproduces HF Qwen3RMSNorm exactly (`self.weight * hidden.to(input_dtype)` = cast-then-multiply, verified in transformers 5.13.1 source). Bit-exact (max_diff 0.0) vs host fp32 ref at [4,2560] gamma[2560] and per-head [128,128] gamma[128] (exp4). Per-head QK norm needs no special casing: kernel derives cols from gamma.dim[0] | Yes |
| 2026-07-12 | Exp 4 resolved: `nn.gather[axis=0]` is the embedding lookup; nothing to write | bit-exact vs host ref at tiny shape incl. vocab-edge id 255 (exp5) | Yes |
| 2026-07-12 | Upstream WMMA bug already filed: modular/modular#6722 (2026-06-26, Needs Triage) | covers f16/bf16 selection failure on gfx1201; our FP8 finding (different error: operand legalization) is NOT in it -- comment drafted at .agent/notes/upstream-6722-comment.md, awaiting go-ahead to post | Yes |
| 2026-07-12 | Attention written by hand (warp-per-query, fp32 softmax); vendored RDNA attention unusable | WMMA blocker (above). Decode is bandwidth-bound so no WMMA needed for G1a-2 | Yes |
| 2026-07-12 | Logits kept in FP32, but greedy argmax runs on BF16-ROUNDED logits with first-index tie-break | HF's lm_head emits bf16 and torch.argmax takes the first max; comparing in fp32 resolves ties torch never saw and picks differently. Fixed prompt3 step7; took teacher-forced step match to 20/20 | Yes |
| 2026-07-12 | RoPE applied in BF16 (cos/sin cast to bf16), not fp32 | HF returns `cos.to(dtype=x.dtype)` and rotates in bf16; computing in fp32 is more accurate and therefore drifts from the reference we must match | Yes |
| 2026-07-12 | REJECTED: rounding QK^T scores to bf16 before scaling (literal eager arithmetic) | measured worse -- prompt2 divergence moved 12 -> 6 against both references. Reference's effective precision exceeds its Python source; measure, don't assume | No |
| 2026-07-12 | **G1a-1 is under-specified: HF's sdpa and eager disagree with each other** (steps 4/32/7 on our 3 prompts) | every divergence is a 1-bf16-ulp near-tie (one is an exact tie broken only by argmax index order). "Token-identical to HF" names no single target past ~4 tokens. Architect decision needed -- see .agent/notes/g1a1-reference-ambiguity.md | No |
| 2026-07-12 | Forward pass status: prompt1 TOKEN-EXACT 256/256 vs oracle; prompts 2/3 match 12 and 106 tokens; tiny 3x16 exact; 20/20 teacher-forced steps | structurally correct; residual ~1 ulp drift flips near-ties | No |
| 2026-07-12 | G1a-1 restated as a three-part checkable gate in SPEC.md v1.1 (teacher-forced fidelity + one full autoregressive match + decision-boundary-not-full-vocab bound); PASS | "token-identical vs HF" named no single target -- HF's sdpa/eager disagree with each other and HF's own batched recompute disagrees with its own cached generation; 768-step teacher-forced check found ZERO non-tie argmax flips, measured (13/768 mismatches, all at HF's own exact ties or 1-bf16-ulp gaps) | No |
| 2026-07-12 | **Phase 1b entry gate: root-cause or affirmatively characterize the isolated logit spikes (12.06 @ prompt1/step67/token96874, plus 4 smaller: ~5.14/4.15/1.75/1.55) before FP8 work begins** | Spikes are unexplained -- GPU cos/sin precision at large RoPE angles was tested directly and ruled out (measured error ~1e-6, six orders of magnitude too small). FP8 E4M3 has a 3-bit mantissa vs BF16's 7; any latent numerical fragility behind these spikes has far less room to hide at that precision and could manifest as argmax-relevant, not just tail-token, divergence. First diagnostic: reproducibility test at the exact (step, token) pair -- deterministic across repeated runs (implicates legitimate but sensitive floating-point compounding, characterizable and possibly benign) vs. non-deterministic (implicates a race condition or uninitialized-memory read, must be fixed regardless of FP8). Phase 1b cannot be declared started until this is resolved one way or the other | No |
| 2026-07-12 | G1a-2: 3 launch-count reductions applied, each re-verified against a NEW decode-path teacher-forced check (the original 768-step check uses one big prefill and never exercises M=1 gemv, so it could not have caught a regression here) | (1) K/V linear projections write directly into the KV cache, removing the separate cache_write copy kernel; (2) residual add fused into o_proj/down_proj via the matmul kernels' elementwise_lambda_fn epilogue (verified every call site invokes it at width=1 before relying on that); (3) Q+K RoPE fused into one kernel launch (index-range branch, no new closures). 19 -> 14 launches/layer. 47.98 -> 49.13 tok/s (+2.4%). All 9 resulting decode-path teacher-forced mismatches individually checked against HF's own logits: 5 exact ties, 4 at exactly 1 bf16 ulp, zero non-tie flips | Yes |
| 2026-07-12 | REJECTED: fusing gate_proj+up_proj into one matmul (contiguous in the arena for layer 0) | checked ALL 36 layers before relying on it -- layer 35 is NOT contiguous (shard-boundary staging artifact). Using this without the check would have silently corrupted layer 35's up_proj output. Not implemented | No |
| 2026-07-12 | Fused QK-norm kernel built (same index-branch pattern as RoPE fusion), measured ~49.2-49.3 tok/s, then REVERTED | every individual mismatch it introduced re-verified as tie/1-ulp (10/10, zero non-tie flips) -- but it changed WHICH prompt hits near-ties, and prompt1 (G1a-1 part (b)'s "one full clean run" evidence) started diverging at step4. None of 3 prompts stayed fully clean under it. Correct individually, but broke a specific already-PASSing piece of evidence -- reverted rather than re-litigate G1a-1 for ~0.15 tok/s. Logged to IDEAS.md as Phase 2 (batching changes the reduction order anyway, and per-request tie-sensitivity to one prompt's evidence matters less there) | Yes (reverted; code never committed) |
| 2026-07-12 | G1a-2 status: 49.13 tok/s = 89.1% of llama.cpp's 55.14 raw, 99.0% of the 49.63 (90%) target, short 0.50 tok/s. Stays UNCHECKED | stopped after 3 verified launch-count reductions + 1 built-and-reverted attempt; gap is characterized as fixed per-kernel-launch overhead (75%+ bandwidth utilization already, not a bandwidth wall). Path to closing it is real profiling to find the next-largest per-step cost -- a different, larger-scoped kind of work than launch-count reduction, not attempted this session | No |
| 2026-07-12 | Phase 1a status: G1a-1 PASS, G1a-3 PASS, G1a-2 at 89.1% with a characterized gap | forward pass correct, loader fast, speed deficit understood but not closed. Two items still open before Phase 1a can be reviewed for closure: the tail-logit-spike investigation (Phase 1b entry gate, this file 2026-07-12) and the G1a-2 profiling pass. Neither attempted yet | No |
| 2026-07-12 | **Phase 1b entry gate CLEARED** — logit-spike investigation closed; RoPE stepwise-bf16 fix merged to main; residual full-vocab spikes characterized as irreducible BF16 matmul reduction-order ambiguity (not a defect) | Probes 0–15: deterministic; RoPE f32-accum was a real bug (cut-verified fix); post-fix residual is 110 torch-vs-sequential-f32 ULPs on q_proj amplified by ill-conditioned rows — Heph is bit-exact textbook f32-accum. Not benign under strict FP8 argmax parity, but Phase 1b gates are tolerance-based (PPL, speed) against a fully-understood BF16 baseline. Full writeup: `.agent/notes/spike-investigation.md` | Partially (RoPE fix reversible; characterization stands) |
| 2026-07-13 | Measured launch-overhead directly (not estimated): 4.06us/dispatch queued (matches production), 36.15us individually-synced. At 504 dispatches/token: ~2.05ms/token (~10%) | experiments/g1a2_launch_overhead.mojo; confirms launch overhead is real but not the dominant cost -- the ~5us/~12% hypothesis was close but launch overhead alone can't explain the full gap | No |
| 2026-07-13 | Per-category profiling (src/qwen_profile.mojo) caught a sampling bug in its own first run: profiling only 30 early steps (n_keys 1-31) hid attention's true cost, since attention's inner loop is O(n_keys) and production averages over a full 256-step run where n_keys reaches ~265. Re-profiled over all 255 steps: attention jumped from 6.9% to 24.0% of profiled time -- the second-largest category, previously invisible | measure the SAME distribution production measures, not a convenient subset; caught before any conclusion was drawn from the biased sample | No |
| 2026-07-13 | G1a-2 root cause: matmuls (66-71% of profiled time) already achieve 340-565 GB/s effective bandwidth (at/above the ~512GB/s peak citation) -- no headroom without WMMA (blocked). Attention's score-computation loop was the addressable cost: O(n_keys), single-warp, serial | back-calculated effective bandwidth per matmul category from known weight-tensor byte counts; ruled out matmuls as a target BEFORE picking attention as the fix, rather than guessing | No |
| 2026-07-13 | FIX: attention's score-computation loop (`scores[j] = scale*dot(q,k_j)`) strided across 4 warps instead of 1 -- each scores[j] is independent (no cross-j dependency), so this computes every score identically, just concurrently. Softmax and weighted-V-sum stay single-warp, UNCHANGED, bit-for-bit. 49.13 -> 54.22 tok/s (+10.4%) | explicitly does NOT touch reduction order (unlike the reverted QK-norm fusion): only independent per-key computations were parallelized; accumulation (softmax normalization, PV sum) was left untouched on purpose | Yes |
| 2026-07-13 | Correctness re-verified as bit-identical, not just within-tolerance: decode-path teacher-forced counts identical (256/253/250) pre- and post-change; prompt1 full autoregressive still 256/256 | a stale-baseline confusion was caught mid-verification: prompts 2/3's full-autoregressive divergence steps (6, 7) looked different from OLDER cited numbers (12, 106) that turned out to be from BEFORE the 3 launch-count fusions, not this branch's true prior state. Isolated with git stash + rebuild: the true immediately-preceding baseline ALSO diverges at steps 6/7, confirming zero behavior change, not a regression | No |
| 2026-07-13 | **G1a-2: PASS.** 54.22 tok/s avg (3 reps: 53.970, 54.323, 54.354) = 98.3% of llama.cpp's 55.14 raw, 109.2% of the 49.63 (90%) target. 85.2% bandwidth utilization (up from 75.4%). Measured BEFORE the RoPE stepwise-bf16 fix landed on main; re-verify after merge | SPEC.md G1a-2 box checked. Stopped here per instruction -- gate cleared with room to spare; further attention tuning (more warps, or restructuring the weighted-V-sum, which WOULD need reduction-order changes to parallelize) is Phase 2 territory | No |
| 2026-07-13 | Merged g1a2-profiling (multi-warp attention) with main's RoPE stepwise-bf16 fix -- disjoint kernels (attention vs rope_kernel/rope_kernel_qk), clean auto-merge in kernels.mojo, no code conflict | both branches forked from f26c35b and touched disjoint kernels; re-verification after merge required regardless since the RoPE fix changes numerics upstream of attention | No |
| 2026-07-13 | Phase 1a status: **G1a-1 PASS, G1a-2 PASS, G1a-3 PASS. Phase 1b entry gate CLEARED.** All three exit-gate criteria met, both parallel workstreams (G1a-2 profiling, logit-spike investigation) closed and merged to main | Phase 1a ready for closure review | No |
| 2026-07-13 | GPU greedy sampling via custom `argmax_logits` (not vendored `nn.argmaxmin_gpu`) | Host linear scan was 51.6ms/token vs ~18ms forward (bench/1a-ab Finding 2). Vendored topk/argmax returned higher index on exact ties (experiments/argmax_gpu_probe.mojo); wrote one-block warp+shared reduction with lowest-index-on-tie. Measured ~0.30ms/step; total 256-token run 18.0s→4.94s; decode incl. sampling ~52.8 tok/s. Wired into generate/tfd/ab_bench/tiny | Yes |
| 2026-07-13 | Chunked safetensors load_arena (128MB stream + per-chunk DtoH byte verify) | Cleaner host memory; stronger than old sampled full-blob round-trip. Does **not** reduce peak VRAM (see next row) | Yes |
| 2026-07-13 | Peak VRAM ~29.6GB CHARACTERIZED as MAX/AsyncRT device pool reservation, not loader bloat | First DeviceBuffer of any size reserves ~29.56GB (~90% of R9700). DeviceContext alone ~270MB. Closed runtime; no env knob found (vram_isolate_probe{,2}). Chunked loader does not change this. Not a leak (returns to ~75MB after exit) | No (runtime behavior) |
| 2026-07-13 | **WMMA BLOCKER RESOLVED -- supersedes the 2026-07-12 row.** Both BF16 and FP8 E4M3 WMMA execute correctly on gfx1201 via **direct `llvm_intrinsic`**, bypassing the Mojo stdlib entirely | Root cause: `mma_amd_rdna.mojo` emits the RDNA3 16-element fragment encoding; gfx12 requires 8-element fragments (A/B as `<2 x i32>` for FP8, `<8 x i16>` for BF16) and takes **no i1 operands** on the f32-accum forms. Verified against `IntrinsicsAMDGPU.td` and the clang gfx12 builtins. Evidence: `experiments/exp3g_RESULTS.md`, `exp3g_fp8_wmma_gfx12.mojo` (PASS), `exp3g_bf16_wmma_gfx12.mojo` (PASS) | Yes |
| 2026-07-13 | Activation quantization (bf16 -> e4m3, per-token absmax) is **BUILT and validated**, not pending scope | RDNA4 WMMA is fp8xfp8 -> f32 only (no mixed bf16xfp8 operand mode -- confirmed by enumerating the LLVM gfx12 WMMA builtins, not only third-party report), so W8A8 is forced by the ISA. Teacher-forced 4B: 748/768 = **97.4%** argmax vs HF oracle; layer-diff noise uniform (max/median ~4.5), not outlier-shaped. Branch `fp8-wmma-decode` | Yes |
| 2026-07-13 | **FP8 WMMA is not a decode speedup on this hardware.** Decode at M=1 is bandwidth/issue-bound, not coalescing-bound | Measured on GPU 0: direct-global **56.6** tok/s (~227 GB/s), LDS-staged **36.8**, weight pre-swizzle **39.8**. Both coalescing fixes REGRESSED -- a 16-wide matrix instruction computing one useful row is issue/latency-bound. Roofline 640 GB/s => ~159 tok/s ceiling; llama.cpp Q8_0 co-measured 109.5. Independently corroborated by the ESDMAX write-up on the same silicon. **Open decision:** shape-matched architecture (WMMA for matrix-shaped work incl. batched decode; streaming dot for M=1) | No (measured) |
| 2026-07-19 | Frank G1b-0 FP8 reachability probe **NOT run -- premise superseded** | The probe asked whether FP8 WMMA can fire on gfx1201; it has been firing since 2026-07-13. The author read `main`, which still carried the 2026-07-12 blocker as standing fact and did not contain `exp3g`. **Process fix adopted: any DECISIONS entry that is later overturned must be marked SUPERSEDED in place, not merely followed by a newer row.** Research preserved verbatim on branch `frank-fp8-research` | Yes |
| 2026-07-19 | **M=1 decode is structurally under-parallelized -- not a tuning deficiency.** `grid=(N/16,)` launches 256 waves for q_proj and only **64** for k/v_proj on a machine that hosts thousands | Little`s Law: saturating the measured 569 GB/s at ~400ns latency needs ~228 KB in flight; 256 waves x one 8-byte load is ~2 KB. Even a 4x K-loop unroll reaches ~8 KB -- still ~28x short. **Per-wave tuning cannot reach the roofline at M=1.** This one fact explains the split-K, LDS and swizzle regressions AND the 8x concurrency scaling. Derivation: `.agent/notes/2026-07-19_decode-wave-arithmetic.md` | No (derived + measured) |
| 2026-07-19 | **Priority set: FP8 WMMA prefill GEMM ahead of all further M=1 decode work** | FP8 prefill is 87 tok/s vs BF16 v3a ~1400 -- a 16x gap that is a **missing kernel** (the FP8 M>1 path row-loops the gemv), not a dtype problem. Prefill is compute-bound where FP8 doubles throughput (383 vs 191 TFLOPS), it is pillar-neutral (no BF16 compute, no architecture ruling needed), and it is the Odysseus TTFT pain directly. Ladder: 87 -> ~1400 (kernel) -> up to ~2800 (FP8 edge). Not llama`s 8134 -- residual is kernel maturity, no longer a gate | Yes |
| 2026-07-19 | **LDS asymmetry is a named trap: keep LDS for prefill, drop it for decode** | M>1 reuses each staged weight tile across output rows, amortizing the barrier (v3a BF16, proven 793 tok/s). M=1 uses every weight exactly once, so barriers are pure overhead (measured 36.8 vs 56.6). Porting the decode kernel no-LDS shape into the prefill GEMM would produce a confusing regression | No (measured both directions) |
| 2026-07-19 | **Graph capture is unavailable from Mojo on AMD** | No `hipGraph` anywhere in the Mojo GPU stdlib; MAX CUDA-graph support lives in the Python pipeline layer, not the layer this engine is written against. Documented win elsewhere is 14-20%% at small batch; we measure ~720 launches/token = 16-19%%. **Launch reduction must therefore come from kernel fusion** -- which has broken correctness twice here (QK-norm, gate+up): one fusion at a time, teacher-forced after each | No (toolchain) |
| 2026-07-19 | **Achievable bandwidth on gfx1201 is 569.2 GB/s, not the 640 spec; `rocm-smi` UMC%% is not a bandwidth proxy** | 569.2 GB/s measured by the RDNA4 llama.cpp fork on this silicon. Our FP8 decode at ~227 GB/s is **40%% of achievable** -- the honest headroom figure. A 6%% UMC reading alongside ~227 GB/s of real traffic is a duty-cycle sampling artifact. Also: Navi 48 has a 64 MB Infinity Cache, so any microbenchmark re-reading a small tile measures LLC, not VRAM | No |
| 2026-07-19 | **Concurrency probe: 8 independent processes scale 8.0x linearly (56.6 tok/s each, 453 aggregate); ceiling is VRAM from weight duplication (OOM at M=12)** | A **hardware-capacity probe, not a product configuration and not a competitive claim** -- each process holds its own ~4GB weight copy, which is neither shippable nor how llama.cpp serves concurrency. The Pillar-2 comparison is against `llama-server --parallel 8 --cont-batching` (one weight copy, slots) and is **unmeasured**. What it does establish: large GPU idle capacity at M=1, and weight duplication -- not compute -- as the ceiling, which is exactly what fused batching removes | Yes |
| 2026-07-20 | **llama.cpp best-in-class is an envelope across TWO backends, not one number.** Co-measured Vulkan vs ROCm (same commit 33ca0dcb9, same Q8 model, +0.21% thermal drift): Vulkan decode +16% single-stream (127.06 vs 109.39) and 1.15-1.23x through npl=8; **ROCm wins at npl=16** (962.30 vs 873.52, 0.91x). Vulkan trails 7-10% on batched prefill. No feature gap -- batched-bench, FA, KV footprint (2304 MiB) identical | The competitive target at our Pillar-2 gate point (npl=8) moved **23%** upward: 677.83, not 549.65. Our decode position widened from 0.60x (vs ROCm) to **0.52x** (vs Vulkan). Quoting ROCm-only numbers after this measurement would be cherry-picking. Caveat: ROCm pp512 stddev is 11-17% (cold first rep/process) vs Vulkan 1.8% -- **do not quote the llama-bench prefill "tie"**, it is inside ROCm noise; the batched prefill numbers are the trustworthy read. `bench/llamacpp-vulkan-baseline.md` | No (measurement) |
| 2026-07-20 | **The two backends differ in scaling SHAPE, not just level -- and that is the reusable finding.** ROCm %roofline: 81-72-70-59-**59** (hits a bound and holds). Vulkan: 93-87-84-73-**53** (never bounds, keeps degrading, surrenders a 23% lead between npl 8 and 16) | Read: RADV is the better *kernel* runtime; ROCm has the better *batching* story. **Phase 2 IS the batching story**, so ROCm's flat tail is the behavior to study and Vulkan's collapse is the behavior not to reproduce. A design that wins at npl=8 and collapses at 16 has reproduced Vulkan's failure with extra steps | No (measurement) |
| 2026-07-20 | **SUPERSEDES the 460 GB/s "practical ceiling" used in handoff `e379cdd`.** Vulkan measured **530.7 GB/s at npl=1** -- 93.3% of the synthetic 569 -- on the same mixed weights+KV workload. 460 was **ROCm's implementation limit, not a hardware-practical ceiling** | Two consequences. (1) Strengthens the M=1 thesis: at 93.3% of synthetic roofline single-stream there is essentially nothing left there for anyone -- "no 2x hiding at M=1" is now measured, not argued. (2) Raises the concurrency prize: ~531 GB/s is demonstrably reachable while the best batched result is 416.5 (Vulkan npl=8), a ~22% gap that is the Phase 2 opportunity in measured terms. **All gates state % of 569 -- one denominator, matching both bench tables** | No |
| 2026-07-20 | **Gates are stated as % of roofline, not tok/s -- adopted as standing convention.** Tok/s targets move when upstream lands a patch (llama got faster at every re-measure this project); a percentage does not. Report the tok/s ratio as headline, the percentage as the progress metric | Direct fix for the moving-goalpost problem that cost us a 23% goalpost shift mid-phase. Phase 2 carries **two gates, labeled and not merged**: pillar gate = >=3x aggregate at npl=8 vs our own npl=1 (*does the Multiplier multiply*); competitive gate = beat the envelope per point, 73% @ npl=8 and 59% @ npl=16 (*is it implemented well*). These can diverge -- 3x self-relative at 40% of roofline is a real possible outcome | No (convention) |
| 2026-07-20 | **FP8-KV cache: quality PASS, negative-controlled. Enters Phase 2 as designed-in architecture.** Simulated (E4M3 quant->dequant at cache-write, BF16 storage) rather than implemented -- separating the *question* from the *build*. 4K self-A/B 4093/4096 = **99.93%**; capacity **1.992x** (147456 -> 74016 B/token) | Verification closed two ways the PASS could have been fiction: token counter calibrated linearly (16->1152, 32->2304), ON@4K = 294912 = 4096x36x2 exactly, OFF = 0; E4M3 grid assert 0 violations ON vs **282,047,598** OFF. Divergence shape healthy -- 3 sparse deterministic near-tie flips, no tail clustering, no snowball. **The simulation is now the bitwise oracle: the real cache is correct iff it reproduces `85879df` exactly.** FP8 *weights* by comparison give ~5% and ~1.7 slots (marginal) -- KV is where the capacity thesis lives. `bench/fp8-kv-verification.md` | Yes (behind comptime flag, default off) |
| 2026-07-20 | **Every gate gets a negative control that can FAIL -- adopted as standing convention.** An assertion that passes in both states is itself a no-op | 282M violations with the flag OFF is what makes 0-violations-ON mean anything. Corollary learned the same day: **instrument what you mean, not what is convenient.** Call granularity here is per-layer x per-{K,V} x per-forward-chunk (4608 host launches for a 4K walk), so an *invocation* counter would have read three orders of magnitude low and looked exactly like a no-op; counting **tokens covered** is robust to vectorization. Same class as the exp3g all-ones test that could not validate lane mappings | No (convention) |
| 2026-07-20 | **Anomalies are recorded WITH their resolution, and hypotheses are labeled as hypotheses** | 4K self-A/B diverges ~5.3x *less* than 512 -- backwards from naive error accumulation. Resolution recorded rather than left open: (a) the earlier 768-token oracle delta (5 tokens) and the 512 self-A/B divergence (2 tokens) use different token sets AND different axes (net-of-oracle vs self-A/B), so they were never comparable and their failure to reconcile is not a contradiction; (b) the 512 window is a *prefix* of the 4K sequence, so this is a density comparison within one sequence (2/512 early, 1/3584 after); (c) the lower-entropy/wider-margin explanation is **a hypothesis consistent with the data, not proven** -- n=3 is far too small to generalize. Honest statement: error accumulation is not the dominant pattern *here*, not that divergence decreases with context | No |
| 2026-07-20 | **Small-M no-LDS FP8 decode-batch GEMM (2≤M≤32) recovers GEMV-class bandwidth; the prior 0.5x-of-llama gap was the v3a LDS path at thin reuse, not failed amortization.** Kernel: BM=16/BN=64/BK=16, 1 wave/block, no LDS staging on weights, edge-masked M. Dispatch: M=1 gemv, 2≤M≤32 small-M, M>32 v3a prefill (LDS), all path-isolated | M=8 full-forward jumps **~28%/274 -> 53.0%/519 tok/s** of 569 (recheck 53.5%); curve is monotonic 50.2->51.3->53.0->59.2% and **crosses above** the M=1 GEMV by M=16, exactly the arithmetic-intensity prediction (M MACs/byte vs 1). Confirms the 2026-07-19 LDS-asymmetry entry as a *prediction*: prefill kernel run at decode-batch M regresses precisely as warned. Correctness bit-exact vs v3a (`max_abs_diff=0`, gate `!=0`) across every routed shape incl. K=9728 down_proj and N=151936 lm_head; prefill M=512 unchanged (~1693 vs 1702, path tags v3a). **Caveat: forward-only, past=0 -- optimistic register.** Headline ~519 tok/s ~0.95x llama ROCm npl=8 softens toward ~0.85x once per-token sampling is added, and gentles further with context (KV bandwidth is per-stream, does not amortize across M). Generation step yields the real end-to-end numbers. `bench/small-m-decode-gemm.md` | Yes |
| 2026-07-20 | **Concurrency-shape probe (`.agent/specs/2026-07-20_concurrency-shape-probe.md`) returned a NULL measurement, not a finding -- filed with its cause.** N independent forward_fp8 on ONE in-order HIP stream serialize by construction (step times 20/40/81/161 ms = N x single-stream); aggregate pinned at single-stream rate regardless of kernel/memory behavior | The probe as specified had no concurrent submission in it -- I ratified a design whose output was determined by its plumbing. Contradicted by the 2026-07-19 8-process result (8.0x linear, 453 agg): the memory system *does* overlap when work is actually submitted concurrently. Superseded immediately by the fused M-row probe, which asked the right question (does M-row fusion amortize weights? yes). Lesson: a probe must be able to return a value other than the one its submission model forces. Null-probe harness (`src/qwen_concurrency_shape_probe.mojo`, `bench/concurrency-shape-probe.md`) left uncommitted | No (null) |
